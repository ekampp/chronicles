!!!

%html(lang="en")
  %head
    = stylesheet_link_tag "application"
    %script(type="text/javascript" src="//use.typekit.net/fhe8ibr.js")
    %script(type="text/javascript") try{Typekit.load();}catch(e){}
    %link{ href: asset_url("favicon/favicon.png"), rel: "shortcut icon" }
  %body
    %header.structure.header
      = render "layouts/menu"
      = render "layouts/flashes"

    %section
      = yield

    / Javascript
    = javascript_include_tag "application"
